# Database
# Local (host) Postgres
DATABASE_URL="prisma+postgresql://username:password@localhost:5432/snowshare"
# Docker Compose (uncomment to use inside containers)
# DATABASE_URL="postgresql://postgres:postgres@db:5432/snowshare"

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here"

# Auth

ALLOW_SIGNUP=true
NODE_ENV=development

# Upload directory
# Set to an absolute path to store uploaded files
# Defaults to ./uploads in project root if not set
UPLOAD_DIR=

# Telemetry (Plausible Analytics - privacy-friendly, GDPR compliant)
# Set to "false" to disable analytics
TELEMETRY=true
# Custom Plausible settings (optional - defaults to SnowShare's instance)
# PLAUSIBLE_DOMAIN=your-domain.com
# PLAUSIBLE_HOST=https://your-plausible-instance.com

# Security - Email Verification (optional - can be configured via admin panel)
# SMTP settings for sending verification emails
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM_EMAIL=noreply@snowshare.local
# SMTP_FROM_NAME=SnowShare

# Security - CAPTCHA Protection (optional - can be configured via admin panel)
# Supported providers: recaptcha-v2, recaptcha-v3, turnstile
# CAPTCHA_ENABLED=false
# CAPTCHA_PROVIDER=recaptcha-v2
# CAPTCHA_SITE_KEY=your-site-key
# CAPTCHA_SECRET_KEY=your-secret-key